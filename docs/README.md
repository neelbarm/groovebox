# docs

`screenshot.png` is referenced from the project README.

To regenerate it:

```bash
npm run web          # http://localhost:4173
```

Generate a lo-fi track, press play, and capture the player mid-playback
(waveform + spectrum + chord ribbon all lit up). Save it here as
`screenshot.png`, ideally around 2000px wide.
