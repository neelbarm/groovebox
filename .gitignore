node_modules/
dist/
*.log
.DS_Store

# Rendered audio is regenerated by `npm run demo` -- keep the repo light.
examples/*.wav
*.wav

# ...but the tiny, reviewable artefacts are worth versioning.
!examples/*.mid
!examples/*.mp3
!examples/*.json
